import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import norm

#range between -4 and 4 with 0.1 steps
x_axis = np.arange(-4,4,0.1)
#mean = 0 standard deviation = 1
plt.plot(x_axis, norm.pdf(x_axis,0,1))
plt.show()


